package com.mysticat.roleplay.ui

/**
 * 应用内更新日志：**只保留当前版本**，历史版本走
 * [AppLinks.CHANGELOG_DOC_URL] 的金山文档，避免应用内越积越长。
 *
 * 为什么两端各一份常量：两个平台**版本号独立编**（Android 与桌面各走各的版本线），
 * 各自发的版、各自记的账；且桌面首版的内容里有"桌面特有"的条目（便携包替换、系统朗读走
 * Windows 语音等），混在一份里会误导用户。运行时按 [Platform.ui] 的 `platformId` 取其一。
 *
 * 更新方式：发版时改写对应那份（桌面发版改 [DESKTOP]，Android 发版改 [ANDROID]）。
 */
internal object Changelogs {

    /** Android 当前版本的内置更新日志（对外日志的精简版，`· ` 前缀） */
    const val ANDROID = """0.2.2-alpha.2（2026-09-25）
· 「角色卡」页升级为「宝库」，世界书独立成册：顶部「角色｜世界书」两段切换；世界书是账号级独立资产，可单独新建 / 导入导出 JSON / 删除，每本书显示被几张卡使用，多张卡可共用同一本书，随备份 JSON 一起迁移
· 导入酒馆（SillyTavern）卡时自带的世界书会自动带进来，并自动抽成独立的书
· 世界书条目批量管理：全选 / 启用所选 / 停用所选 / 删除所选，整理几十条词条不用一条条点
· 世界书新增「从文本生成条目」：把现成的设定文档粘进来，按空行 / 按行 / 按标题三档批量生成词条，标题可当关键词；全程本地处理不花钱
· 「世界书命中」面板未命中诊断升级：逐条给出原因——没写关键词 / 正则写错 / 大小写 / 全词匹配 / 副关键词没凑齐 / 近似关键词提示（如"艾瑟兰"写成"艾瑟林"）
· 语音服务页新增「新回复自动朗读」开关（与聊天页顶栏喇叭是同一个开关）
· 修复「工具 / 玩法」形态的卡没有自动朗读（顶栏喇叭消失、开了也不出声）——现在四种形态都能自动朗读
· 点页面空白处即可收起键盘、退出输入状态
· 修复说明文字里 **粗体** 与 `等宽` 标记原样露出星号（9 处）；聊天气泡支持粗斜体 / 斜体 / 代码块、粗体嵌套不再失效
· 字体设置显示自定义字体的名字；应用顶层按返回键改为"再按一次退出鲸鱼""""

    /** Windows 桌面版当前版本的内置更新日志（发版时替换成对外日志的精简版） */
    const val DESKTOP = """1.2.0（2026-09-25）
· 世界书独立成册（上一版"世界书暂不支持"已解决）：账号级独立资产，可单独新建 / 导入导出 JSON / 删除，每本书显示被几张卡使用，多张卡可共用同一本书，随备份 JSON 一起迁移
· 「角色卡」页升级为「宝库」：第二栏顶部「角色｜世界书」两段切换，选中书后第三栏整版编辑器
· 导入酒馆（SillyTavern）卡时自带的世界书会自动带进来，并自动抽成独立的书
· 世界书条目批量管理：全选 / 启用所选 / 停用所选 / 删除所选
· 世界书新增「从文本生成条目」：把现成的设定文档粘进来，按空行 / 按行 / 按标题三档批量生成词条，标题可当关键词；全程本地处理不花钱
· 「世界书命中」面板未命中诊断升级：逐条给出原因——没写关键词 / 正则写错 / 大小写 / 全词匹配 / 副关键词没凑齐 / 近似关键词提示
· 语音服务页新增「新回复自动朗读」开关（与聊天页顶栏喇叭是同一个开关）
· 修复设置页「改了没生效、甚至被改回去」：从别处改完设置回到设置页不再念旧值，此时点保存也不会把别处刚改的项静默改回去
· 修复聊天页顶栏「自动朗读」图标不跟着设置变（只影响显示）；修复「工具 / 玩法」形态的卡没有自动朗读
· 点页面空白处即可退出输入状态
· 修复说明文字里 **粗体** 与 `等宽` 标记原样露出星号（9 处）；聊天气泡支持粗斜体 / 斜体 / 代码块、粗体嵌套不再失效
· 字体设置显示自定义字体的名字"""


    /** 按平台取当前版本的日志文本 */
    val current: String
        get() = if (com.mysticat.roleplay.ui.Platform.ui.platformId() == "desktop") DESKTOP else ANDROID
}
