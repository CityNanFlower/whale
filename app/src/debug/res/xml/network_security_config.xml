<?xml version="1.0" encoding="utf-8"?>
<!--
  仅调试包生效（release 不含本文件）：
  允许连「本机假服务端」（10.0.2.2 = 模拟器眼里的宿主机、localhost/127.0.0.1），
  用来在没有余额、不发真实请求的前提下验证网络链路（真流式、重试、兜底解析等）。

  ⚠️ 2026-09-17 起：main 的 manifest 也开了 `usesCleartextTraffic="true"`（理由见那段注释：
  本地部署的语音服务、服务端返回的 http 资源链接本来就要走明文），所以这个 debug 专用文件
  现在只是"把本机假服务端也显式列出来"，**不再是唯一的明文放行来源**。
  真正的门禁在 App 代码里：对话/创作/生图过 requireHttps、语音那一路只放行回环与私有网段、
  远端资源链接一律升级成 https 再取（`AiClient.httpsUrl`）。
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
